

#Default the security credentials to call the config service. Override these
#for your deployment.
direct:
  webservices:
    security:
      basic:
        user:
          name: admin
          password: d1r3ct;

  config:
    service:
      url: http://localhost:8082/

# Override these properties for your own security credentials.
  configui:
    security:
      user:
        name: admin
        password: direct #'{bcrypt}$2a$04$/ICdCjClVtkhXabFY0Uqlu7uuVc56ACbd3ShX9OVEFpKgSFhxiWJC'

spring:
  application:
    name: direct-config-ui

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

  main:
    allow-bean-definition-overriding: true

#unique jmx names to avoid collisions when running
#multiple spring boot apps in an web container
endpoints:
  jmx:
    default-domain: direct-config-ui
    uniqueNames: true

spring.jmx.default-domain: direct-config-ui

server:
  tomcat:
    max-part-count: 30